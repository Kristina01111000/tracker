<html lang="en-US">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tracker program</title>
    <script src="tracker-script.js" async></script>
    <style>
    body {
        width: 100vw;
        height: 100vh;
    }
    .circle {
        position: absolute;
        bottom: : 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border-radius: 50%;
    }
    .circle1 {
        position: absolute;
        bottom: : 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid red;
        border-radius: 50%;
    }
    .circle2 {
        position: absolute;
        top: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid green;
        border-radius: 50%;
    }
    .circle3 {
        position: absolute;
        bottom: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid blue;
        border-radius: 50%;
    }
    .circle4 {
        position: absolute;
        top: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid purple;
        border-radius: 50%;
    }
    .circle5 {
        position: absolute;
        bottom: : 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid yellow;
        border-radius: 50%;
    }
    .circle6 {
        position: absolute;
        top: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid orange;
        border-radius: 50%;
    }
    .circle7 {
        position: absolute;
        bottom: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid black;
        border-radius: 50%;
    }
    .circle8 {
        position: absolute;
        top: 1px;
        width: 50px;
        height: 50px;
        color: transparent;
        border: 6px solid grey;
        border-radius: 50%;
    }

    </style>
</head>


<body>
    <div class="circle"></div>
    <div class="circle2"></div>
    <div class="circle3"></div>
    <div class="circle4"></div>
    <div class="circle5"></div>
    <div class="circle6"></div>
    <div class="circle7"></div>
    <div class="circle8"></div>
</body>


<script type="text/javascript">
const AREA = document.body;
const CIRCLE = document.querySelector('.circle');
const CIRCLE2 = document.querySelector('.circle2');
const CIRCLE3 = document.querySelector('.circle3');
const CIRCLE4 = document.querySelector('.circle4');

const CIRCLE5 = document.querySelector('.circle5');
const CIRCLE6 = document.querySelector('.circle6');
const CIRCLE7 = document.querySelector('.circle7');
const CIRCLE8 = document.querySelector('.circle8');
//actual height and width of the viewport
var windowWidth = window.innerWidth;
var windowHeight = window.innerHeight;

function mouseCoordinates(e) {
    // Capture the event object (mouse movement).
    // Get X coordinate (distance from left viewport edge) via clientX property.
    // Take total window width, subtract current coordinate and width of circle.
    // Do the same for Y coordinate (distance from top viewport edge).
    var horizontalPosition = windowWidth - e.clientX - 26;
    var verticalPosition= windowHeight - e.clientY - 26;
    var horizontalPosition2 = windowWidth - e.clientX - 200;
    var verticalPosition2= windowHeight - e.clientY - 200;
    // Set horizontal and vertical position.
    CIRCLE.style.right = horizontalPosition + 'px';
    CIRCLE.style.bottom = verticalPosition + 'px';
    CIRCLE2.style.left = horizontalPosition + 'px';
    CIRCLE2.style.top = verticalPosition + 'px';
    CIRCLE3.style.left = horizontalPosition + 'px';
    CIRCLE3.style.bottom = verticalPosition + 'px';
    CIRCLE4.style.right = horizontalPosition + 'px';
    CIRCLE4.style.top = verticalPosition + 'px';

    CIRCLE5.style.right = horizontalPosition2 + 'px';
    CIRCLE5.style.bottom = verticalPosition2 + 'px';
    CIRCLE6.style.left = horizontalPosition2 + 'px';
    CIRCLE6.style.top = verticalPosition2 + 'px';
    CIRCLE7.style.left = horizontalPosition2 + 'px';
    CIRCLE7.style.bottom = verticalPosition2 + 'px';
    CIRCLE8.style.right = horizontalPosition2 + 'px';
    CIRCLE8.style.top = verticalPosition2 + 'px';
}

function changeColor(e){
	//change the color of the border of circles when the mouse is clicked
	CIRCLE.style.borderColor = white;//"#"+((1<<24)*Math.random()|0).toString(16);
}



// When the mouse moves, run the mouseCoordinates function.
AREA.addEventListener('mousemove', mouseCoordinates, false);
// when the mouse moves run the change color function
//AREA.addEventListener('onmousedown', function() (CIRCLE.style.borderColor = white;}, false);//changeColor, false);

</script>
</html>
